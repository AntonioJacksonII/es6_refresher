function printMyName(name, age) {
    console.log(name, age);
}

printMyName('Antonio', 32);

const printName = (name, age) => {
    console.log(name, age);
}

printName('Tony', 32);

const multiply = (number) => {
    return number * 2
}

console.log(multiply(2));

const double = number => number * 2;

console.log(double(2));